name: transfer-bonuses

# Scrape travel-on-points.com for current transfer bonuses and snapshot-replace
# the transfer_bonuses table in Postgres. Runs twice monthly; can also be
# triggered manually (with optional dry-run mode).
on:
  schedule:
    # 9am UTC on the 1st and 15th of each month.
    - cron: "0 9 1,15 * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Fetch + parse, but skip DELETE/INSERT"
        type: boolean
        default: false

# Least-privilege: only needs repo checkout; DB credential via secret.
permissions:
  contents: read

# Never let two runs overlap on the shared Postgres table.
concurrency:
  group: transfer-bonuses
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
      # nodriver drives Chrome via CDP — install a real Chrome binary.
      - uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable
      - run: pip install -r requirements.txt
      - name: Scrape and reconcile transfer bonuses
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          BETTERSTACK_SOURCE_TOKEN: ${{ secrets.BETTERSTACK_SOURCE_TOKEN }}
          BETTERSTACK_INGEST_URL: ${{ secrets.BETTERSTACK_INGEST_URL }}
          BETTERSTACK_LOGS_TOKEN: ${{ secrets.BETTERSTACK_LOGS_TOKEN }}
          BETTERSTACK_LOGS_INGEST_URL: ${{ secrets.BETTERSTACK_LOGS_INGEST_URL }}
          # Optional Better Stack heartbeat — a missed run then raises an alert. No-op if unset.
          BONUSES_HEARTBEAT_URL: ${{ secrets.BONUSES_HEARTBEAT_URL }}
        run: python transfer_bonuses.py ${{ inputs.dry_run && '--dry-run' || '' }}
